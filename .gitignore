# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Jupyter
.ipynb_checkpoints/

# Environment
.env
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Output
*.csv
*.parquet

# Certificates / secrets
*.pem
*.pfx
*.key

# Customer / engagement artifacts — never commit
*KPMG*
*kpmg*

# Archives (avoid committing binary bundles / assessment exports)
*.zip

# Terraform state & vars (may contain secrets) — keep only *.example
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
tfplan

# Browser automation / MCP session logs
.playwright-mcp/

# Local scan / scratch files
_*.txt
_*.zip

# Runtime artifacts / exports / deploy records (may contain org identifiers)
**/out/
**/.deployments.json
**/deploy.log
**/*.log

# Backup of ADO inner git dir (kept locally for ADO sync)
fabric-sdlc-governance/_git_ado_backup/

fabric-sdlc-governance/.sources.env
fabric-sdlc-governance/.run.log
fabric-sdlc-governance/.bulk.log
fabric-sdlc-governance/out_alldomains.txt
fabric-sdlc-governance/*.log

fabric-cicd/.bulk.log
fabric-cicd/*.log

